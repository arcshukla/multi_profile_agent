[pytest]
testpaths = tests
addopts   = --tb=short -q
# Keep temp dirs only for failed tests; successful test dirs are deleted immediately.
# This ensures no test data accumulates on disk between runs.
tmp_path_retention_policy = failed
tmp_path_retention_count  = 3
